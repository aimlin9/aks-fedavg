# Core federated-learning / encryption stack (proposal Section 4E)
flwr>=1.8,<2.0          # Flower — federated learning framework (Beutel et al., 2020)
torch>=2.2,<3.0
torchvision>=0.17,<1.0
numpy==1.26.4
pandas==2.2.1
phe==1.5.0              # python-paillier — Paillier homomorphic encryption

# Stats / plotting for the baseline-vs-engineered comparison (Wilcoxon test, Pareto plots)
scipy>=1.12
matplotlib>=3.8

# Optional — only needed for the DP-augmented extension (future work, Section 8)
opacus>=1.4,<2.0
